<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aldodokter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aldodokter">
        <activity android:name=".controller.activity.Register"></activity>
        <activity
            android:name=".controller.activity.MyCustomAppIntro"
            android:label="My Custom AppIntro"></activity>
        <activity android:name=".controller.activity.DetailAnimal" />
        <activity android:name=".controller.activity.HomeActivity" />
        <activity android:name=".controller.activity.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>